import Typography from "typography"

const typography = new Typography({
  baseFontSize: "16px",
  baseLineHeight: 1.5,
  scaleRatio: 3,
  headerFontFamily: ["Inter-Regular", "sans-serif"],
  bodyFontFamily: ["Inter-Regular", "serif"],
})

export default typography
